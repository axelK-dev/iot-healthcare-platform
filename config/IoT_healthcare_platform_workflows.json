{
  "onboarding_steps": [
    { "step": "Authenticate (OAuth2/JWT)" },
    { "step": "Present & capture consent (policy_version)" },
    { "step": "Collect minimal patient fields (data_minimization)" },
    { "step": "Verify device identity (voucher + trust_chain)" },
    { "step": "Activate monitoring (RBAC-enforced)" }
  ]
}